sudo npm init -y(package.json)
sudo npm install tailwindcss@latest
npx tailwindcss init --full(tailwind.config.js)
npx tailwindcss init(tailwind.config.js)

parallax

add firebase
sudo curl -sL firebase.tools | upgrade=true bash
